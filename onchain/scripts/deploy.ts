/**
 * Hardhat / viem deploy stub. Replace with your tooling of choice.
 *
 *   1. Deploy SlotVault
 *   2. Deploy SlotSpin(oracle, vault)
 *   3. vault.setSpin(spin.address)
 *   4. Seed vault with bankroll: vault.depositLP{ value: X }()
 *   5. Oracle backend starts committing serverSeeds.
 */
export {};
